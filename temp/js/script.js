// Custom JavaScript for IJIRCST Website
// This file is currently empty as the provided screenshot
// primarily depicts a static layout.
// You can add interactive elements here if needed in the future.

// Example: A simple script to log when the page is loaded
document.addEventListener('DOMContentLoaded', () => {
    console.log('IJIRCST website loaded successfully!');
});

// Example: Smooth scrolling for anchor links (if you add them)
// document.querySelectorAll('a[href^="#"]').forEach(anchor => {
//     anchor.addEventListener('click', function (e) {
//         e.preventDefault();
//         document.querySelector(this.getAttribute('href')).scrollIntoView({
//             behavior: 'smooth'
//         });
//     });
// });
